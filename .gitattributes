# Set the default behavior to automatically normalize line endings
* text=auto

# Explicitly declare text files you want to always be normalized to LF on check-in
*.java text
*.xml text
*.properties text
*.md text

# Denote all files that are truly binary and should not be modified
*.jar binary
*.class binary
